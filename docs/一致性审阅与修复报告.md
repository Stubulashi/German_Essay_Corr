# Spec 一致性审阅与漏洞修复报告

> 审阅日期:2026-09-17 · 审阅范围:仓库根规格文件 `.cursorrules`、`.qoder/skills/german-essay-corrector/`(SKILL / ARCHITECTURE / OPERATIONS)、`docs/UI规范.md`,以及全部后端与前端代码。
> 本报告随附交付:①离线自包含运行能力;②班级合并功能;③本清单中的全部修复。验证记录见文末。

---

## 一、Spec 逐项核对结论

### 1. `.cursorrules` 第 5 节「系统功能与路线图」

**标记为【已实现】的 4 项核心功能(全部核对通过)**:

| Spec 项 | 核对结论 |
|---|---|
| 批量异步队列(FastAPI + asyncio 工作池、ZIP/多图、孤儿自愈) | 已实现:`queue_service.py` + `correction_service.recover_orphaned_tasks` + 上传两阶段校验/护栏 |
| 班级共性错因分析(高频错因 Top、典型错例、建议顺序、得分分布) | 已实现:`/api/analytics/class-diagnosis` + 前端班级分析页 + 讲评摘要 |
| Human-in-the-Loop 复核 UI(分屏、可编辑报告) | 已实现:审阅页(原图 | OCR 复核 | 教师版/学生版/批注核对 | 编辑保存/恢复/打印) |
| 学生错题本(复现错因、时间线、个体分布) | 已实现:`/api/students/profile` + 前端学生错题本页 |

**【后续候选(路线图)】逐项结论**:

| 路线图项 | 结论 | 依据 |
|---|---|---|
| 教师校准与反馈闭环 | 部分实现 | 报告编辑保存(校准)已上线;"示范学习"后端本次已交付(示例范文风格归纳→注入后续批改),前端页面待排期 |
| 自定义评分细则(Rubric) | 未实现 | 未启动;建议后续与提示词微调统一设计 |
| 批注模式全文视图 | 已实现 | `AnnotatedTranscript` + `GET /api/tasks/{id}/annotation` |
| 错误↔行号联动定位 | 已实现 | 后端区间定位(`locate_error_spans`)+ 前端内联批注 |
| HEIC 直传与客户端压缩 | 部分实现 | HEIC 直传已实现(pillow-heif 可选组件 + 明确降级提示);上传前客户端压缩待前端排期 |
| SSE 实时进度 | 未实现 | 当前采用轮询(审阅页 2s / 队列页 3s),满足单机场景 |
| 数据保留与备份 | 已实现 | `备份数据.bat`/`恢复数据.bat` + 启动版本守卫 + 班级数据包导出导入 |
| 多用户认证(可开关) | 未实现 | 本地单用户定位;设置中心"回环+可选令牌"权限加固方案已批准待实施 |
| 双管线交叉校验 | 未实现 | 未启动 |
| 订正追踪 | 部分实现 | 学生版订正单/报告已上线;订正回填追踪未启动 |
| 桌面化打包 | 部分实现 | 本次"离线自包含运行"达成零安装开箱可用(内置运行时+静态托管);桌面壳(Electron/Tauri)未做 |
| 可观测性增强 | 部分实现 | 结构化日志/健康检查/队列状态已有;专项(指标/审计)未启动 |

### 2. Skill 文档与 ARCHITECTURE / OPERATIONS
- 双管线契约、错因标准化、统一 Schema、Markdown 模板、状态机、迁移约定与代码**逐项一致**;
- 本次已补充:UI 规范与内置使用指南约定(SKILL.md)、离线运行与班级合并说明(见本次文档同步)。

### 3. `docs/UI规范.md` 与实现一致
- theme Token、PageHeader、响应式断点、文案规范均与代码一致(该文档经用户审阅修改,以其为准)。

---

## 二、本次修复的问题清单(位置 / 方式 / 行为影响)

| # | 问题 | 等级 | 位置 | 修复方式 | 行为影响 |
|---|---|---|---|---|---|
| 1 | 新增班级合并路由使用 `Query` 但未导入 → 应用启动即 `NameError`,后端完全无法启动 | 严重 | `app/api/routes_classes.py` 导入区 | 补充 `Query` 导入(冒烟测试捕获) | 修复性,无 |
| 2 | HEIC 经预处理转码为 JPEG 后仍沿用 `.heic` 扩展名 → 传输层 MIME 推断与实际内容不一致 | 中 | `routes_corrections._save_upload` / `_save_bytes` | 新增 `_sniff_ext()` 按内容嗅探真实格式并修正扩展名(失败保持原扩展) | 常规 JPG/PNG 路径无变化;HEIC 更正确 |
| 3 | 合并迁移包含未命名外键(SQLite 批量模式下 downgrade 必然失败) | 中 | `migrations/versions/b38655eebf98` | 显式命名 `fk_classes_merged_into`(升级/降级同步) | 升级路径不变,downgrade 可用 |
| 4 | 「作业台账」「示范学习」后端已实现但未注册路由/未接线 → 功能不可达、风格缓存不加载 | 高 | `app/main.py` | 注册 `routes_ledger`/`routes_style`;lifespan 加载生效风格缓存 | 新端点可用;既有端点/行为不变 |
| 5 | (测试暴露)回滚后 ORM 对象过期,测试内同步访问 `src.id` 触发懒加载 | 低 | `tests/test_class_merge.py` | 合并前捕获 id 供断言使用 | 无产品影响;回滚语义验证保留 |
| 6 | 早前修复回顾:`_cleanup_saved` 空目录改为深度优先删除;`parse_roster_text` 补单空格分隔 | 低 | `routes_corrections.py` / `student_assignment.py` | 已在本轮之前的开发中完成 | 修复性,无 |

**常规审阅结论(未发现新问题项)**:SQL 注入(全 ORM 参数化)、命令注入(无 shell 调用)、ZIP 路径穿越(`Path(...).name` + `_safe_relative`)、解压炸弹(条目数/总量/单条三层护栏)、XSS(无 `dangerouslySetInnerHTML`/`rehype-raw`)、迁移一致性(当前版本 = head `b38655eebf98`)。

---

## 三、未修复项与残余风险(明确结论)

1. **`.env` 密钥明文**(接受):本地文件定位;"设置中心 + 数据加密"方案已批准待实施,届时收敛;
2. **CORS 白名单仅 localhost:5173 / 无速率限制**(记录):回环部署下低风险;若开放局域网需先落地令牌方案;
3. **路线图未启动项**(Rubric / SSE / 认证 / 双管线交叉校验 / 桌面壳 / 可观测性专项):不阻塞当前使用,见第一节结论表;
4. **单进程假设**(记录):队列与风格缓存为进程内实现,请勿以多 worker 方式启动(标准启动脚本符合该假设)。

---

## 四、验证记录

- **回归测试**:`pytest` 全量 **142 passed**(含本次新增班级合并 5 项;既有用例零回归);
- **前端构建**:`npm run build`(tsc + vite)通过;
- **离线开箱冒烟**(便携运行时):`runtime\python\python.exe` 启动后端 →
  `GET /`(200,静态托管前端)、`/api/health`、`/api/style/status`、`/api/ledger/items`、`/api/classes`、`/api/classes/merge/preview`、`/api/style/profiles` 全部 200;
- **环境自检**:`一键自检.bat`(即 `backend/scripts/selfcheck.py`)全部 [OK],退出码 0;
- **迁移**:`alembic current == heads == b38655eebf98`。

---

## 五、第二轮补齐(2026-09-18):全量 Spec 审阅与实施结果

对 `plans` 目录本项目全部 7 份规格逐份核对并**补齐全部未完成项**(设置中心改造与增强、安全加固与数据加密、统一统计层、示范学习前端、作业台账/考试统计前端、上传优化前端),打通跨模块数据链路(三源读时聚合 + 学生对齐合并 + 加密透明链路)。

- 详细对照表、关键改动与验证记录见 **《Spec 完成度对照与交付说明.md》**;
- 本轮后验证:后端 **203 passed**(零回归)、前端构建通过、端到端冒烟 18/18 端点 200、一键自检全部 [OK];
- 迁移 head 更新为 `e42a079e0596_add_app_settings`(新增 `app_settings` 表);
- 上文“三、残余风险”中第 1 项(`.env` 密钥明文)已按批准方案收敛:敏感项脱敏回显/可选加密(密钥包裹存储,`.env` 不再新增明文密钥),访问控制落地“回环限制 + 可选令牌”。
