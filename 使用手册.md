# 德语作文批改系统 · 使用手册

> **适用读者:使用本系统批改作文的德语老师(不需要任何电脑基础)**
> 只要照着本手册一步一步做,就能完成批改。遇到看不懂的词,请翻到第十章"小词典"。
>
> 本手册中的【双击】= 用鼠标左键快速连点两下;【点击】= 用鼠标左键点一下。

---

## 目录

- 第一章 快速了解:这个系统是什么
- 第二章 第一次使用(一辈子只做一次)
- 第三章 每天的使用:就两步
- 第四章 手把手:批改第一份作文
- 第五章 批改一整个班的作文(批量模式)
- 第六章 人工复核(两条管线均可开启)
- 第七章 看懂批改报告
- 第八章 五大页面逐一详解
- 第九章 常见问题解答(FAQ)
- 第十章 小词典
- 第十一章 进阶:让系统连接真实的 AI 模型
- 第十二章 数据安全与备份
- 第十三章 忘记怎么用了?打开「使用指南」
- 第十四章 合并班级(两位老师共管一个班)
- 第十五章 在别的电脑上使用(离线运行与自检)
- 第十六章 更多功能一览:台账 · 考试 · 统计 · 示范学习 · 设置中心与加密
- 附录 文件与文件夹说明

---

## 第一章 快速了解:这个系统是什么

### 1.1 一句话说明

你把手写德语作文**拍照或扫描**后传给这个系统,它会自动**认字、批改、打分、写出批改报告**,
还会帮你分析**整个班最容易错的地方**,并且记住**每个学生反复犯的错误**。

**三个典型使用场景:**

1. **收上来的作文太多批不完**:一个班 40 份作文,批量上传后去喝茶,回来看报告;
2. **备课讲评没头绪**:系统自动生成"讲评摘要",告诉你这个班最该讲哪几个语法点;
3. **想知道某个学生到底哪里一直不会**:打开他的"错题本",反复出错的地方一目了然。

### 1.2 系统一共有九个页面

| 页面 | 作用 | 一句话理解 |
|---|---|---|
| 批改工作台 | 上传作文、开始批改 | "交货口" |
| 批改队列 | 查看所有任务的进度和结果 | "进度墙" |
| 批改审阅 | 一份一份仔细看报告、修改、打印 | "办公桌" |
| 班级管理 | 建班、名单、手写模型、数据包、合并与班级错因分析 | "班主任手册" |
| 学生错题本 | 单个学生的错误历史 | "病历本" |
| 作业台账 | 日常作业登记与汇总 | "记分册" |
| 考试统计 | 考卷识别与班级考试分析 | "考场登记" |
| 统计分析 | 三边成绩汇总的总表与图表 | "教务汇总" |
| 示范学习 | 让系统学会你的批改风格 | "照葫芦画瓢" |

后四个是新增的,详细用法见第十六章。

### 1.3 两个一定要记住的"黑窗口"

启动系统后,桌面会出现**两个黑色的小窗口**(写着"批改系统-后端"和"批改系统-前端")。
它们是系统真正在"干活"的地方:

- **使用期间绝对不要关掉它们**(把窗口拖到屏幕边角就好);
- 用完了、要关电脑前,双击"一键停止.bat"再关,最安全;
- 万一不小心关了黑窗口,系统就用不了了 —— 没关系,重新双击"一键启动.bat"即可恢复。

---

## 第二章 第一次使用(一辈子只做一次)

### 2.1 先认识"批改器"文件夹

你的电脑上有一个叫 **`批改器`** 的文件夹(如果还没有,请向系统管理员索取)。
打开它,会看到三个最重要的文件:

| 文件名 | 什么时候用 |
|---|---|
| **首次安装.bat** | 一辈子只用一次(第一次使用这台电脑时) |
| **一键启动.bat** | 每次开始使用系统时双击它 |
| **一键停止.bat** | 每次用完系统时双击它 |

> **重要提醒:请把"批改器"文件夹整个放在一个固定的位置(例如 D 盘根目录),不要频繁移动它。**

### 2.2 情况一:电脑已经帮你配置好了

如果这台电脑之前已经安装并使用过本系统(管理员告诉你的),**请直接跳到第三章**,
以后每天只需要用"一键启动.bat"和"一键停止.bat"两个文件。

### 2.3 情况二:全新电脑(需要先装两个免费软件)

本系统需要电脑上先装好两个免费软件:Python 和 Node.js。**只需装一次,以后不用再管。**

**第一步:安装 Python**

1. 打开浏览器,访问:https://www.python.org/downloads/
2. 点击页面上的黄色大按钮"Download Python"(下载最新版);
3. 下载完成后,双击安装包开始安装;
4. **最关键的一步**:安装第一个界面左下角有一个小方框,写着
   "**Add python.exe to PATH**"(把 Python 加入环境变量)——
   **必须打勾!必须打勾!必须打勾!**
5. 然后点击"Install Now",等它装完,点"Close"关闭。

**第二步:安装 Node.js**

1. 打开浏览器,访问:https://nodejs.org/zh-cn
2. 点击下载"**LTS(长期支持版)**"的 Windows 安装包;
3. 双击安装包,**一路点击"下一步"(Next)→"我接受"→"下一步"…直到"完成"**,
   什么选项都不用改。

**第三步:检查是否装好**

回到"批改器"文件夹,双击"**首次安装.bat**":

- 如果屏幕显示"**环境检查通过**",说明两个软件都装好了,继续往下;
- 如果提示"[错误] 这台电脑还没有安装 Python/Node.js",
  说明上一步没装成功,请重新执行对应步骤(**特别注意 Python 的 Add to PATH 打勾**)。

### 2.4 双击"首次安装.bat"(只做一次)

双击"**首次安装.bat**"后,会弹出一个黑色窗口,请**耐心等待 3~10 分钟**
(它在下载安装系统运行所需的零件,只需要这一次):

```
[第 1 步] 检查电脑环境...
   环境检查通过。
[第 2 步] 安装后端程序(第一次需要几分钟,请耐心等待)...
[第 3 步] 安装前端界面(第一次需要几分钟,请耐心等待)...

==============================================
   安装完成!
   以后每天使用:双击"一键启动.bat"
==============================================
```

**看到"安装完成!"四个字,就大功告成了。** 按任意键关掉这个窗口。

> 如果中途提示"安装失败",通常是网络问题。请检查网络后,**重新双击"首次安装.bat"** 即可,
> 它会把没装完的部分接着装好,不会重复劳动。

---

## 第三章 每天的使用:就两步

### 3.1 第一步:双击"一键启动.bat"

双击"**一键启动.bat**",会弹出一个窗口,依次显示:

```
[1/3] 启动后端服务(会新弹出 1 个窗口,请不要关闭它)...
[2/3] 启动前端界面(会再弹出 1 个窗口,请不要关闭它)...
[3/3] 等待大约 10 秒,然后自动打开浏览器...
```

**总共会看到三个窗口**(一个"启动"窗口 + 两个"批改系统"黑窗口),都是正常的。

大约 10 秒后,**浏览器会自动打开系统的界面**。
如果浏览器没有自动打开,请自己打开浏览器,在地址栏输入:

```
http://localhost:5173
```

按回车,就能看到系统页面了。

> **看到浏览器里出现"智能批改工作台"几个大字,就说明启动成功了。**

### 3.2 第二步:像用普通网页一样使用

浏览器打开后,左侧有导航菜单:**批改工作台 / 班级管理 / 批改队列 / 学生错题本**。
具体怎么操作,见第四章(手把手批改)和第八章(页面详解)。

使用期间请记住:

- 那**两个"批改系统"黑窗口不能关**(可以最小化);
- "启动"那个窗口,看到"启动完成"后按任意键关闭即可;
- 一次启动可以**连续使用几个小时**,批很多份作文不用重新启动。

### 3.3 用完关闭:双击"一键停止.bat"

用完系统后(或下班关电脑前),双击"**一键停止.bat**":

```
   正在停止德语作文批改系统...

   系统已停止。
```

然后**把剩下的黑窗口点右上角的 X 关掉**即可。
(不执行这一步直接关电脑一般也没事,但养成好习惯更稳妥。)

### 3.4 关于"演示模式"的特别说明(重要)

刚拿到系统时,界面顶部可能出现一个**黄色的提示条:演示模式(Mock)**。

- **演示模式的含义**:系统还没有连接真实的 AI 模型,批改出来的内容是**样例数据**
  —— 它是为了让您先熟悉界面和操作流程用的,**不是真的批改结果**;
- 在演示模式里,所有按钮、流程(上传→批改→复核→报告→班级管理→错题本)都可以**完整体验**;
- 要真正批改学生作文,需要连接真实的 AI 模型,请见**第十一章**(通常需要管理员协助配置一次)。

---

## 第四章 手把手:批改第一份作文

> 本章假设你已经完成第三章第 1、2 步(浏览器里已经打开了系统)。

**第 1 步|进入批改工作台**

点击左侧菜单的"**批改工作台**"。
你会看到中间写着"**智能批改工作台**"几个大字,左边是上传区域,右边是"批改参数"卡片。

**第 2 步|选择上传模式**

在上传区域顶部,有两个按钮:"**单篇批改**"和"**批量批改**"。
批改单个学生的作文请选"单篇批改"(它默认就是选中的)。

> 什么时候用批量批改?学生人数多的时候,见第五章。

**第 3 步|选择批改管线(用哪套 AI)**

右侧"批改参数"卡片里有两张可选卡片:

- **管线 A · 本地 VLM**(零成本 / 高隐私):用学校自己架设的模型,免费,作文图片不出校园,
  但速度取决于学校服务器快慢;**(学校已部署本地模型时,推荐用它)**
- **管线 B · 云端解耦**(高精度 / 深度推理):用云端的商业 AI,识别最准、速度较快,
  但需要联网并且按用量计费;**同样支持"人工复核"(见第六章)**。

**不知道选哪个?问一下学校管理员,或者在两台都试过后凭感觉选。**
点击卡片即可选中(卡片边框变成蓝色、右上角圆圈变蓝)。

**第 4 步|选择评分标准**

在"2. 评分标准"这一行,有两个按钮:

- **高考 25 分制**:按中国高考德语作文标准打分(25 分制);
- **DSD / CEFR 等级**:按 DSD 考试标准评估,给出 A1/A2/B1/B2 等级结论。

**学生备考什么考试,就选哪个。**

**第 5 步|选择批改细致度**

在"3. 批改细致度"这一行,有三个按钮:

- **低 · 标错**:只把错误的地方标出来,适合快速浏览;
- **中 · 标错+修改**:标出错误并给正确的写法,**日常批改推荐**;
- **高 · 保姆级**:标错 + 修改 + 用中文讲解"为什么错",适合重点作文和考试讲评。

**第 6 步|填写班级和作业信息(可选,但强烈建议)**

在"4. 班级与作业(可选)"区域:

1. **归属班级**:点开下拉框,选择这个学生所在的班级;
   - 如果下拉框里还没有班级,点击旁边的"**+**"按钮,输入班级名称(例如"高二(3)班德语"),
     点"创建"即可,以后就一直有了;
2. **作业名称**:填一下这份作文是什么作业,例如"第一次月考作文"(以后在错题本和分析里能按作业区分);
3. **作文题目/要求**:可以把作文题目粘进去(可不填)。

> **为什么要填班级?** 填了以后,"班级管理 → 共性错因分析"才能按班级统计;不填也能正常批改。

**第 7 步|上传作文图片**

1. 把作文拍好照(手机拍也行,尽量拍正、光线足、不要有手指遮挡);
2. 把图片传到电脑上(微信文件传输助手、数据线拷贝、U 盘都行);
3. **直接拖拽图片到页面上的虚线框里**(或者点击虚线框,从电脑里选择图片文件);
4. 如果一个学生的作文有**两页**,就把两页都拖进去(系统会当作一份作文);
5. 上传后,下方会显示已选择的文件列表,可以点右侧的"X"删掉传错的图。

> 支持的图片格式:JPG、PNG、WEBP、BMP。一张照片最好不要超过 10MB。
> 如果用了"批量批改"模式,还可以直接传 ZIP 压缩包。

**第 8 步|点击"开始批改"**

确认参数和图片都没问题后,点击右下角大大的"**开始批改**"按钮。

**第 9 步|等待批改完成**

页面会自动跳转到"批改审阅"页面,右侧显示:

```
正在批改中...
当前阶段:识别转录 / 评分分析 / 生成报告
```

阶段会自己往前走,**请不要刷新或关闭页面**,耐心等待:

- 本地模型(管线 A):通常 1~5 分钟;
- 云端模型(管线 B):通常 1 分钟以内;
- 如果配置了"人工复核",它会先停在"待人工复核"(见第六章)。

**第 10 步|查看批改报告**

批改完成后,页面右侧会出现完整的**批改报告**(左边是作文原图,右边是报告)。
报告怎么看,请见第七章。

**第 11 步|(可选)修改报告**

如果觉得报告的某些话想改改(比如措辞、分数),点击报告上方的"**编辑**"按钮,
直接在框里像写文档一样修改,改完点"**保存报告**"。
想还原成系统原文,点"**恢复系统原始报告**"即可。

**第 12 步|发给学生**

点击报告上方的**下载图标**可以保存成文件;点击**打印图标**可以直接打印机打印。
发给学生建议用"**学生版**"(见第七章 7.2)。

**恭喜!你已经完成了第一份作文的全部批改流程!**

---

## 第五章 批改一整个班的作文(批量模式)

### 5.1 批量上传(和批改一份的流程几乎一样)

1. 进"批改工作台",把上传模式切换成"**批量批改**";
2. 设置好管线、评分标准、细致度、班级(方法和第四章一样);
3. 把**全班每个学生的作文图片**一次性拖进上传区 —— 记住规则:**一张图片 = 一位学生的一篇作文**;
4. 点击"**开始批量批改(N 篇)**";
5. 页面会跳到"**批改队列**",可以看到每一份的进度条。

> 注意:批量模式下,每个学生只放一张图片。如果一个学生有两页,
> 请把两页拼到一张图上(或分别批改后自己合并),不要把两页当成两个学生上传。

### 5.2 用 ZIP 压缩包偷懒

如果图片很多,可以先把它们**放进一个 ZIP 压缩包**,把压缩包直接拖进来也可以 ——
系统会自动把包里的每张图片拆成一个批改任务。

> 压缩包里只有图片(JPG/PNG 等);其他文件会被自动忽略。

### 5.3 在队列页看进度

- **排队中**(灰色):在前面任务后面排队;
- **批改中**(蓝色转圈):正在批;
- **已完成**(绿色):批好了,点最右边的箭头图标看报告;
- **失败**(红色):出了岔子,点"重新处理"按钮重试;
- 页面每 3 秒自动刷新,**不用手动刷新**。

### 5.4 批完以后:导出成绩表

1. 在队列页,**勾选**要给成绩的那些任务(或者不勾选,等于全选当前列表);
2. 点击右上角"**导出成绩表**",会下载一个 Excel 能直接打开的表格文件;
3. 想打包下载全部批改报告,点"**导出报告**",会下载一个 ZIP 压缩包。

---

## 第六章 人工复核(两条管线均可开启)

### 6.1 什么是"待人工复核"

如果你在参数区打开了"**开启 OCR 人工复核**"开关(管线 A 本地 / B 云端均可),
系统会先把作文上的字**认出来给你过目**,等你确认无误后才开始打分。

这样做的好处:AI 认字偶尔会认错(尤其是潦草字迹),由你把关之后,**分数才是建立在正确的原文上**。

### 6.2 怎么复核

1. 任务在队列页会显示黄色的"**待人工复核**"标签,点击箭头进入审阅页;
2. 页面右侧是"**OCR 转录复核**"区域,显示认出来的学生姓名、学号和作文原文;
3. 对照左边的作文原图,把认错的地方**直接改过来**(文本框里改字和写 Word 一样);
4. 改完后点击下方大大的"**确认并继续评分**"按钮;
5. 任务重新排队,等一小会儿(通常不到一分钟),报告就出来了。

> 如果转录几乎没有错误,直接点"确认并继续评分"即可,不用改。

---

## 第七章 看懂批改报告

### 7.1 教师版报告(全量分析)

报告从上到下依次是:

| 部分 | 内容 |
|---|---|
| **标题行** | 学生姓名 / 学号,用的哪条管线、什么标准、什么细致度 |
| **综合得分** | 例如"18 / 25"或"B1 Pass" |
| **总体评价** | 一段总评:优点、主要问题、结构表现 |
| **详细批改** | 一条条列出错误:❌ 表示**错的地方**,✅ 表示**改好的写法**,下面还有中文解析(细致度选"高"才有) |
| **词汇与句型亮点 / 薄弱点** | 写得好的地方 + 最容易错的考点(按出现次数排序) |

> 页面最下方还有"**转录原文**":系统认出的学生原文全文,可以和自己看到的图核对。

### 7.2 学生版:订正单

点击报告上方的"**学生版**"按钮,报告会变成一份适合发给学生的"**订正单**":

- **不显示分数**(分数由您自己公布);
- 一张**订正清单**:每条错误都写明"原文 → 正确写法 → 小提示";
- **做得好的地方**:给学生正面反馈;
- **下一步练习重点**:根据这份作文的错误,自动给出练什么、怎么练;
- **教师寄语**:一句鼓励的话。

点"**下载**"或"**打印**"就可以发给学生了。再点"教师版"可以切换回完整报告。

### 7.3 报告的几个常用操作

| 按钮 | 作用 |
|---|---|
| 下载(↓图标) | 把当前版本报告存成文件 |
| 打印(打印机图标) | 直接打印(自动排版成 A4 纸,只打印报告,不打印网页其他部分) |
| 编辑 | 手工修改报告文字(改完记得"保存报告") |
| 恢复系统原始报告 | 恢复成系统自动生成的原文(您改过之后才出现) |
| 截图里的放大镜/缩小镜/重置 | 调整左边作文图片的大小(也可以直接用鼠标滚轮缩放、按住拖动) |

---

## 第八章 五大页面逐一详解

### 8.1 批改工作台(上传与设置)

- **单篇/批量切换**:见第四、五章;
- **上传区**:拖拽或点击选择图片;
- **参数区四个步骤**:选管线 → 选评分标准 → 选细致度 → 填班级和作业;
- **OCR 人工复核开关**(两条管线均出现):见第六章;
- **开始批改按钮**:一切就绪后点它。

### 8.2 批改队列(任务管理)

- **顶部按钮**:导出报告、导出成绩表、重试全部失败、刷新;
- **筛选栏**:可以按 状态 / 班级 / 起止日期 筛选任务;
- **勾选框**:勾一个或几个任务后,顶部出现"已选择 N 个任务",
  可以"**批量重试**"(重新批)或"**批量删除**"(删掉任务和图片,谨慎使用,删除不可恢复);
- **每行最右侧图标**:箭头=打开报告;循环箭头=重新处理这一条。

### 8.3 批改审阅页(一份的详细视图)

- **左边**:作文原图,可用工具栏放大/缩小/重置,多页作文上方有"第 1 页/第 2 页"标签;
- **右边**:报告(教师版/学生版切换、编辑、下载、打印);
- **顶部彩色标签**:显示这个任务的基本信息(状态、管线、标准、班级、得分)。
  - 看到橙色"**已故障转移 A→B**"标签的说明:本地管线当时网络不通,系统自动改用云端批完了,属于正常保护机制。

### 8.4 班级管理(班主任工作台)与共性错因分析(备课神器)

左侧导航「班级管理」是班级类功能的统一入口,共 6 个标签:总览 / 花名册 / 手写模型 / 数据包 / 班级合并 / 共性错因分析。日常建班、收名单、采集手写样本、迁移数据包都在这里;其中「共性错因分析」标签的用法与以前一样:

1. 最上方筛选:**班级**(选一个班或"全部班级")和**日期范围**;
2. 四张数字卡片:**样本作文、涉及学生、错因总数、平均分**;
3. **错因分布图**:这个班错得最多的考点排行;
4. **得分分布图**:各分数段人数;
5. **错因明细表**:每个考点的次数、涉及人数、典型错例(鼠标悬停在错例上能看到正确写法);
6. **右侧"讲评摘要"**(重点!):系统已经把"该讲什么、按什么顺序讲、举哪个例子"整理好了:
   - 点"**复制**":把内容复制下来,粘到 Word/PPT 里做讲评课件;
   - 点"**下载**":保存成一个文件。

> **使用建议**:批完一个班的作业后,先来这里复制讲评摘要,再对照"错因明细"备课,效率翻倍。

### 8.5 学生错题本(个体追踪)

1. 左边列表可以搜索学生姓名或学号,点击选中;
2. 右侧显示:
   - **头部**:平均分、批改次数、累计错因条数;
   - **橙色警告框"复现错因"**:该学生反复犯的错误(出现两次以上)—— **重点辅导对象**;
   - **个体错因分布图**:他/她个人的薄弱考点排行;
   - **批改时间线**:从早到晚的每次批改,点任何一条可以直接跳转到那份报告。

> **使用建议**:家长会、个别辅导前,先看一眼这个学生的错题本,三分钟了解他的问题。

### 8.6 右上角的小图标

| 图标 | 作用 |
|---|---|
| 月亮/太阳 | 切换深色/浅色界面(晚上用深色不刺眼) |
| 齿轮 | 查看系统当前配置(用的什么模型、是否演示模式) |

---

## 第九章 常见问题解答(FAQ)

**Q1:双击"一键启动.bat"后,浏览器没有自动打开怎么办?**
A:不要慌。自己打开浏览器,在地址栏输入 `http://localhost:5173` 回车即可。
如果打不开,等 20 秒再试一次(系统启动需要一点时间)。

**Q2:浏览器显示"无法访问此网站/无法连接"怎么办?**
A:三种可能,按顺序检查:
1. 那两个"批改系统"黑窗口还在不在?被关掉了就重新双击"一键启动.bat";
2. 黑窗口里有没有红色的"Error"字样?有的话截图给管理员;
3. 多等 30 秒刷新一次页面。

**Q3:任务一直显示"批改中",正常吗?**
A:本地模型(管线 A)批一篇可能要好几分钟,属正常。超过 10 分钟还不动,
进队列页点这条任务的"重新处理"图标重试;批量任务多时,任务在"排队中"等待也是正常的。

**Q4:任务失败了(红色)怎么办?**
A:绝大多数情况点"**重新处理**"再试一次就好。如果反复失败,
把鼠标悬停在任务上看错误提示,或截图给管理员。常见原因:本地模型服务没开、
网络断开、余额/密钥问题。

**Q5:图片传不上去怎么办?**
A:检查三点:① 格式是不是 JPG/PNG(HEIC 等特殊格式先用手机转存成 JPG);
② 单张是否超过 50MB;③ 换个浏览器试试(推荐 Chrome/Edge)。

**Q6:"演示模式"黄色提示条一直在,什么意思?**
A:说明系统还没连真实 AI 模型,批改结果是样例。要正式使用请见第十一章(找管理员配一次即可)。

**Q7:批改的分数和我心里的分数不一样怎么办?**
A:AI 是助教不是法官。点审阅页的"编辑",直接把分数和评语改掉再保存即可;
也可以用"学生版"不给分数的订正单,分数您自己写。

**Q8:我修改过的报告,以后再打开还在吗?**
A:在。保存过的修改版会一直显示。点"恢复系统原始报告"可以随时退回。

**Q9:填错了班级/作业名称,能改吗?**
A:任务上的班级/作业信息在创建后不能直接改。可以到队列页把这条任务"批量删除"(连同图片),
然后带上正确信息重新上传一次。

**Q10:能删除测试用的任务吗?**
A:可以。队列页勾选 → "批量删除"。注意:删除会**同时删掉图片**且**不可恢复**,
正在排队/批改中的任务不会被删除。

**Q11:数据存在哪里?会不会丢?**
A:所有数据都保存在"批改器"文件夹里的 `backend\data` 文件夹中(一台电脑一个库)。
只要不删这个文件夹,数据就在。备份方法见第十二章。

**Q12:换一台电脑还能用吗?**
A:把整个"批改器"文件夹拷到新电脑,在新电脑上先双击"首次安装.bat",再"一键启动.bat"即可,
数据会跟着一起过去。

**Q13:为什么报告里有的错误没有中文讲解?**
A:中文讲解只在细致度选"**高 · 保姆级**"时才有;选"中"只有修改,选"低"只有标错。
需要讲解时重新批一次(参数里选"高")即可。

**Q14:一个学生的作文有两页,怎么处理?**
A:单篇批改模式下,把两页图片一起拖进去(按顺序),系统会当成一份作文处理。

**Q15:批改队列里的任务太多了,怎么找某一个学生的?**
A:用筛选栏:选"班级"、或者用日期范围缩小;再配合浏览器自带搜索(Ctrl+F 不行的话,
可以按学生名在页面里找)。找不到就按班级过滤后翻一翻。

**Q16:"共性错因分析"里没有数据?**
A:它(位于「班级管理」→「共性错因分析」标签)只统计**已完成**的批改任务。请先确认该班有批完的作文,并检查上方的筛选条件
(可能选了一个还没有数据的日期范围)。

**Q17:"学生错题本"里搜不到某个学生?**
A:只有在**批改完成后**系统才会自动给学生建档。确认这个学生的作文已经批完了;
如果是名单按键入了名字、和系统认出来的名字不一致,可以用学号搜。

**Q18:怎么让系统知道"张三"和"Zhang San"是同一个人?**
A:在人工复核时把姓名/学号统一改成规范写法;或者尽量让学生每次把**学号**写在作文上,
系统优先按学号归档,最稳妥。

**Q19:用着用着电脑卡了怎么办?**
A:关闭浏览器里没用的标签页;减少同时批改的数量(批量任务会自动排队,不会真的同时全批)。
本地模型在批改时占用资源较大,批改期间避免打开大型软件。

**Q20:不小心把"一键停止.bat"运行了,但报告还没看怎么办?**
A:数据不会丢。重新双击"一键启动.bat",进队列页找到"已完成"的任务,继续看报告即可。

---

## 第十章 小词典

| 词 | 大白话解释 |
|---|---|
| 管线(管线 A / 管线 B) | "用哪套 AI 来批"的两种方案,A 是本地的(免费),B 是云端的(更准);功能完全一致,仅后端模型不同 |
| 本地模型 | 装在你们学校服务器上的 AI,不联网也能用,作文照片不出校园 |
| 云端模型 | 通过互联网使用的商业 AI(按月/按量收费) |
| OCR | "认字"程序,把图片上的手写字变成可编辑的文字 |
| 转录 | 把图片上的作文一字不差"抄"成文字的过程 |
| 人工复核 | 认字之后先给老师过目修改,再打分 |
| 工况/任务 | 一份作文的一次批改 |
| 批次 | 一次批量上传产生的所有任务的总称 |
| 细致度 | 批改报告的详细程度:低(只标错)/中(标错+改对)/高(还有中文讲解) |
| 评分标准 | 用哪套规则打分:高考 25 分制 或 DSD/CEFR 等级 |
| 故障转移 | 本地 AI 联系不上时,系统自动改用云端 AI 批完(报告上会有橙色小标签) |
| 演示模式 | 没有连真实 AI 时用于熟悉界面的"练习模式",结果是样例数据 |
| 讲评摘要 | 系统自动生成的备课提纲:这个班最该讲什么、按什么顺序讲 |
| 复现错因 | 同一个学生反复犯的错误(错题本里会用橙色标出来) |
| 综合得分 | 这次作文的总分,例如"18 / 25"或"B1 Pass" |
| 转录原文 | 系统认出来的学生作文文字版,用于和原图核对 |
| CEFR / DSD | 欧洲语言等级标准,DSD 是德国官方德语考试,按 A1~C2 分级 |

---

## 第十一章 进阶:让系统连接真实的 AI 模型

> 本章操作需要修改一个配置文件,**建议请管理员协助完成一次**。改好后每天使用无需再动。

### 11.1 演示模式 vs 真实模式

- **演示模式**(`MOCK_MODE=true`):批改结果是样例,只用于熟悉界面;
- **真实模式**(`MOCK_MODE=false`):真正调用 AI 模型批改。

系统当前处于哪种模式,可以点右上角"齿轮"图标查看。

### 11.2 修改配置(记事本三步走)

1. 打开"批改器"文件夹 → 打开里面的 `backend` 文件夹 →
   找到名为 `**`.env**` 的文件,右键 → "打开方式"→ 选择"**记事本**";
   (如果看不到这个文件:在文件管理器上方点"查看",勾选"显示隐藏的项目"和"文件扩展名")
2. 根据需要修改(每行开头有 `#` 的是说明文字,不用动),通常只改这几行:

   ```
   MOCK_MODE=false                ← 关闭演示模式

   LOCAL_VLM_BASE_URL=http://192.168.1.100:8000/v1     ← 学校本地服务器地址(问管理员)
   LOCAL_VLM_MODEL=/models/Qwen3.8-27B-Q4_K_M          ← 本地模型名称(问管理员)

   OCR_API_KEY=你的OCR密钥         ← 云端认字服务密钥(购买后获得)
   DEEPSEEK_API_KEY=你的评分密钥    ← 云端评分服务密钥(购买后获得)
   ```

3. 保存(Ctrl+S)并关闭记事本 → 双击"一键停止.bat" → 再双击"一键启动.bat",新配置生效。

### 11.3 两条管线怎么选(速查表)

| 你的情况 | 建议 |
|---|---|
| 学校已经部署了本地服务器 | 用**管线 A**(免费、隐私最好),需要人工核对认字时对快慢要求不高 |
| 有云端服务的密钥、想要最好效果 | 用**管线 B**,建议打开"人工复核" |
| 两个都有 | 平时用 A 省钱,重要考试用 B |
| 都不确定 | 先用**演示模式**熟悉界面,再问管理员 |

---

## 第十二章 数据安全与备份

1. **数据在哪里**:所有任务、报告、图片都在 `批改器\backend\data` 文件夹里,相当于系统的"仓库";
2. **怎么备份**:关闭系统(一键停止)后,把整个 `backend\data` 文件夹复制一份到 U 盘/移动硬盘即可;
   恢复时,把备份的文件夹拷回来覆盖原位置即可;
3. **多久备份一次**:建议**每月一次**;期末、重要考试后务必备份一次;
4. **不要做的事情**:
   - 不要单独删除 `data` 文件夹里的文件;
   - 不要在没有关闭系统时强行拔 U 盘;
5. **隐私提示**:使用"管线 B(云端)"时,作文图片和文字会发送到云端服务商处理;
   如果学校有数据保密要求,请使用"管线 A(本地)"—— 作文数据完全不出校园。

---

## 第十三章 忘记怎么用了?打开「使用指南」

系统内置了一份**完整的功能说明书**,就在系统里,不需要翻任何外部文件。

1. **怎么打开(三种方式,任选一种)**:
   - 左侧导航栏最下方,点击「使用指南」;
   - 点击右上角的**问号图标**;
   - 打开右上角齿轮「运行配置」后,点最下方的「查看使用指南」。
2. **里面有什么**:按 9 个模块(快速开始 / 批改工作台 / 批改队列 / 批改审阅 / 班级管理 / 学生错题本 / 打印与导出 / 运行配置与双管线 / 常见问题与排查)逐一说明每个功能——它有什么用、什么时候用、怎么操作、关键参数是什么意思、有什么注意事项;
3. **怎么快速找到想要的内容**:
   - 顶部**搜索框**输入关键词(比如"打印""ZIP""复核"),系统会自动筛出相关说明;
   - 左侧**目录**点击任意条目,直接跳到对应章节;
   - 每个模块可点击标题**展开 / 折叠**;右上角还有"全部展开 / 全部折叠"一键按钮;
4. **什么时候看它**:第一次使用某个功能之前;想知道某个按钮是干什么的;想确认某个参数的含义;给新老师交接时,可当培训材料。

---

## 第十四章 合并班级(两位老师共管一个班)

场景:同一个班被两位老师分别建过班级(比如"高二3班"和"高二三班"),数据分散在两个班级里,想让它们合成一个。

1. **在哪里操作**:左侧导航「班级管理」→「班级合并」标签;
2. **操作步骤**:
   - 选择「来源班级」(将被并入的)和「目标班级」(接收数据的),系统自动显示预览;
   - 预览会列出:将搬移的任务数、错因数、花名册人数、台账记录数、考试场数;
   - 花名册里每位学生会标注处置方式:迁入 / 补全学号 / 去重 / 学号冲突(冲突时以目标班级为准);
   - 勾选「我已知晓以上影响」,点「执行合并」。
3. **合并后的效果**:
   - 来源班级的全部数据改归属到目标班级,共性错因分析、学生错题本等处立即按目标班级统计;
   - 来源班级**不会被删除**,只标记为「已并入」,在筛选器中显示并禁用,保证可追溯;
   - 每次合并都会留下记录(来源、目标、各项统计、学号冲突明细),供后续查询;
   - 万一合并过程中出错,系统会**自动完整回滚**——不会出现"合并到一半"的情况。
4. **注意**:合并前建议先到「班级管理 → 数据包」导出一份数据包作为保险存档。

---

## 第十五章 在别的电脑上使用(离线运行与自检)

1. **为什么不需要安装**:项目里已内置"便携式 Python 运行环境"(runtime 文件夹)和前端界面文件,相当于把系统需要的一切都装在了项目文件夹里;
2. **在新电脑上的三步**:
   - 把整个项目文件夹拷贝过去(比如 D:\批改器);
   - 双击「**一键自检.bat**」:屏幕会逐项显示 [OK];若出现 [FAIL],按提示处理;
   - 双击「**一键启动.bat**」:自动打开浏览器(默认 http://127.0.0.1:8765 ;若被占用会自动改用 8766,以提示窗口/地址栏为准);用完双击「一键停止.bat」。
3. **和以前有什么不同**:以前要先装 Python 和 Node.js 才能用;现在只弹出一个服务窗口,浏览器地址也从 5173 变成了 **8765**(界面完全一样);
4. **纯净新电脑验收清单**(第一次部署到新电脑时建议做一遍,全程可断网):
   - 双击「一键自检.bat」:逐项 [OK] —— 现在会额外检查“运行库是否随包内嵌”“密码学/上传组件”“端口占用情况”;
   - 双击「一键启动.bat」:浏览器自动打开;上传一张照片跑一遍完整批改;「一键停止.bat」后再启动,数据还在;
   - 把文件夹换到“带中文/空格”的路径再试一次第 1-2 步;
   - 把整个文件夹拷到 U 盘、再拷到另一台电脑直接启动成功,便携性验收通过。
5. **放置与分发的几个提醒**:
   - 放在桌面、文档或 D 盘这类“能写入”的位置;不要放 C:\Program Files 或写保护的 U 盘(自检会报“上传目录不可写”);
   - 从网盘下载的压缩包若提示“未知发布者/无法验证”,右键 → 属性 → 勾选“解除锁定”后再解压;
   - 端口被占用时:启动脚本先用 8765,被占用自动改 8766,请以提示窗口里显示的地址为准;两个都被占用时按提示先关闭占用程序;
   - 系统要求:Windows 10 / 11(64 位)。
6. **常见问题**:
   - 提示"前端界面文件缺失":说明 frontend\dist 没有附带,需从开发机拷贝; 
   - 想升级:在开发机更新依赖并重新构建,把新的 runtime 或 dist 拷过来即可。

---

## 第十六章 更多功能一览:台账 · 考试 · 统计 · 示范学习 · 设置中心与加密

这些功能在左侧导航栏都能找到,新手可以先用默认设置,需要时再回来查这一章。

### 16.1 作业台账(左侧「作业台账」)

- **快速登记**:选好班级、登记项和日期,全班学生一次登记(等级 A/B/C、分值、做没做、星级四种都可);
- **班级汇总**:学生×项目的表格,每格显示最近一次登记(绿=好、黄=需关注);卡片区显示每项的均分与分布;
- **登记明细**:查历史记录、纠错、删除;
- 没有合适的登记项?右上角「登记项管理」里可新建,或点「一键创建预设」(书面作业/背诵/听写/课堂表现/订正);
- 台账成绩会自动进入学生错题本和统计分析,不需要重复录入。

### 16.2 考试统计(左侧「考试统计」)

1. 新建考试(选班级、填日期与满分);
2. 进入详情页「上传考卷」——每个文件一名学生,文件名里带姓名或学号会自动匹配花名册;同名再传一文件就是给同一学生补页;
3. 识别中每 3 秒自动刷新;识别失败的点刷新图标重试;
4. 姓名识别错了点「修订」改正(教师改过的数字不会被重跑覆盖);
5. 全部识别完成后点「生成分析报告」:班级均分、分数段、弱题、知识点失分一页看完,可导出 Markdown。

> 注意:听力部分不纳入统计数据(按计划统一口径)。

### 16.3 统计分析(左侧「统计分析」)

把「作文批改 + 考试 + 台账」三边的成绩汇总在一起(筛选栏可只留其中一两类):

- **成绩总表**:每个学生的综合平均/最近成绩/环比,点任意一行可看全部评估记录;「导出总表 CSV」用 Excel 打开;
- **成绩分布**:某个来源的分数段柱状图与及格率;
- **排行榜**:综合平均榜、最近考试榜、进步榜、需要关注的学生、台账覆盖率;
- **趋势**:班级月度平均曲线;填学生姓名或学号可对比个人轨迹。

说明:综合平均 = 三类成绩都折算成 0~100 分后的等权平均;CEFR 等级只计不参与平均。

### 16.4 示范学习(左侧「示范学习」)

想让系统模仿你自己的批改风格?三两步做到:

1. 先照你想要的标准批改一篇样板作文(工作台正常批改即可);
2. 到「示范学习」页,选这篇已完成的任务,点「归纳并启用」;
3. 之后所有批改都会带上你的风格(评分尺度、点评语气、修改偏好、表达习惯);工作台会显示「示范学习生效」小标签;
- 同一时间只有一个风格生效;不想要了点「停用」,立刻回到默认风格;还能编辑风格描述做微调。

### 16.5 设置中心与数据加密(顶栏齿轮图标)

- 改成了**大窗口**样式:左边是“大范围”(基础运行 / 管线 / 上传 / 教学与报告 / 安全等),右边是该范围下的具体小项,一页看完;
- **运行模式**在窗口右上角:正常 / 演示(样例数据不调用模型)/ 开发(放开全部高级配置项);
- 配置项直接改、点「保存」就生效;带「需重启」标记的项重启后端才生效;管线分组里的「测试连接」按钮可检查模型服务连不连得上;
- **教学与报告**(新):及格线(默认 60,统计/排行榜/考试报告统一口径)、学生版是否显示得分、学生版是否保留“下一步练习重点”;
- 「探索」小标签的项可单独「恢复默认」,底部「回退」可一次性撤销本批改动;「探索性实验项」是按分类聚合的快捷入口;
- **大模型超参数与自动适配**(新):每条管线所属分组里可逐项调整 temperature / top_p / max_tokens / presence_penalty / frequency_penalty / seed / stop / response_format / stream,三态可选「auto」(自动适配,默认)/「omit」(不发送该参数)/ 具体值;端点报「参数不支持」(如 kimi-k3 不支持 temperature)时系统会自动剔除该参数、重试并记住,同一端点+模型的后续批改直接不再发送;左侧「自动适配记录」面板可查看并一键清除,「自定义参数」可补充任意扩展键值(如 min_p=0.05;repetition_penalty=1.05);
- **提示词微调**与**数据加密**现在是左侧独立面板;
- **数据加密**(可选,学生隐私):设置口令 → 生成恢复密钥(抄下来离线保存)→ 加密存量数据。启用后重启系统需输入口令解锁,界面会自动弹窗;忘记口令可用恢复密钥重置;两个都丢了走 Plan B(先把密文归档,再清空重建)。开发模式可选启用「万能密码」兜底(在 backend\.env 设 DEV_MASTER_ENABLED=true 且 DEV_MASTER_PASSWORD 为 ≥64 位十六进制长 Hash,重启后先正常解锁一次完成登记;仅开发调试用,生产环境必须保持关闭)。
- 设置中心默认只有本机(这台电脑)能改;如需局域网内管理,在设置里配置「访问令牌」。

### 16.6 重新批改(同一份作文重跑)

批完想换个细致度或换个管线再跑一遍?不用删了重传:

1. 打开这篇作文的审阅页(状态:已完成),点标题旁的「**重新批改**」;
2. 弹窗里预填了原配置,想改哪项改哪项(管线 / 标准 / 细致度 / OCR 复核 / 姓名学号 / 班级作业等),没改的自动沿用;
3. 点「开始重新批改」原地重跑,复用原图;刷新页面可看进度与结果。

说明:新的系统报告会覆盖旧报告;你手动编辑过的报告和教师寄语**不会**被覆盖;页面顶部会提示“上次 → 本次”的得分与错因数对比;错题本与统计按新结果重建,不会重复计数。

### 16.7 修改转录原文与看批注核对

- **转录原文**现在就在审阅页左边“作文原图”的下方;识别有错点「编辑转录」直接改,保存后报告与单份文件即时同步;
- 切到「批注核对」视图:左栏全文用**颜色+线型**标出每处错误(拼写红色波浪、语法黄橙色系、更地道的表达绿色虚线等,共19类),右栏是图例与错误清单;
- 点清单里的任意一条 → 左边全文自动定位并弹出批注(原句 / 修正 / 中文解析 / 复制修正);点全文里的标记也一样;键盘 Alt + ←/→ 可上一处/下一处;
- 右上“复制修正”“导出”可把整份批注带走;底部会列出“没能定位到原文”的错误,信息不丢。

### 16.8 上传前编辑图片(工作台)

在批改工作台选好图片后,每张图右侧的铅笔图标进入**图片编辑**:

- **灰度 / 黑白**:打印稿或光线差时一键处理,识别更稳;
- **裁剪**:在图上拖拽框选后点“应用裁剪”;「**姓名栏选区**」一键在顶部生成条带选区,方便裁掉无关背景(说明:姓名识别来自整图 OCR,此选区仅用于裁剪保留,不是单独的识别通道);
- **旋转**:左转 / 右转 90°(拍歪的照片);
- **翻页处理**:“上一张 / 下一张”逐张处理,右上角显示“第 x / y 张”,与文件列表顺序一致;
- **不怕改：**每步可「撤销」,一键「重置原图」回到最初选的文件;只有你点“应用修改”的图片版本会随批改上传,原图不会重复落盘,文件名也不变(文件名匹配指派照常用)。

12. **(新)图片自动压缩**:默认开启,上传时对照片减肥(长边超过 2200px 才缩小,JPEG 质量 88),仅当更小才替换,可在设置中心「上传与图片处理」关闭或调参;

---

### 16.9 练习卷(用真实错因出题)

1. **它是什么**:从「已批改作业」的真实错因数据(错因分布、典型错句、作文主题)生成一份针对性练习卷,并配套**标准答案**;
2. **怎么用**:左侧导航点「练习卷」→「生成练习卷」;
   - 选来源:勾选一次/多次作业(右侧“按班级筛选”缩小范围;或切「全部作业」);列表中会展示每次作业的篇数与高频错因;
   - 选题型(语法填空 / 词汇选择 / 句子改错 / 中译德 / 阅读理解 / 完形填空 / 写作小任务)与题数(5~50);
   - 点「开始生成」:依据错因证据命题,通常数十秒;
3. **生成之后**:进入详情页,顶部可切换「试卷 / 标准答案」两个页签;右上角可「导出试卷/导出答案」(.md 文件)、「打印」(试卷+答案两页,打开即最终纸面效果)、删除(二次确认);
4. **说明**:出题依据只读引用批改数据(与错题本/班级分析同源),不会修改任何统计;题干为德语、解析为中文;若某题型缺少对应素材,会自动用同级别通用考点补齐并在解析中注明。

### 16.10 存档图片压缩(批量瘦身)

1. **上传时自动压缩**(默认开启):照片长边超过上限才等比缩小;JPEG/WEBP 按质量参数重编;PNG 仅做无损优化;仅当结果**更小**才替换(绝不放大);
2. **存量批量压缩**:设置中心 →「上传与图片处理」面板点「压缩存量图片」:后台逐个处理并可看进度,完成时展示总节省量;可随时重跑(已压缩的自动跳过,幂等安全);
3. **参数**(均为设置项):开关、长边上限(800~6000,默认 2200)、质量(50~100,默认 88);
4. **不影响任何流程**:原图路径/文件名不变,审阅、批注、打印、导出、班级包等全部照常;被占用/不可读的文件会跳过,重跑补齐。

### 16.11 扫描 PDF 上传与识别可信度标记

1. **PDF 上传**(手机扫描软件导出的含图 PDF 可直接用):
   - 单篇模式:PDF 各页按顺序 = 同一篇作文的多页;批量模式:每页 = 一篇作文;
   - ZIP 内也可以放 PDF(与图片一样自动展开);单份 PDF 上限 60 页、50MB,展开后的页数计入批量上限;
   - 加密/损坏的 PDF 会给出明确提示,不会静默失败。
2. **识别可信度提示**:识别质量低时(多处模糊),批改后转录面板与复核页顶部会出现醒目提示“识别准确率较低,建议人工核对”,并附上原因;
3. **不确定处标记**:转录中两类标记会被高亮显示,悬停可看提示,便于对照原图回顾——
   - 红底 [unleserlich]:此处无法辨认;
   - 黄底标签(低把握猜测,如 [unsicher:findet]):识别把握不足,显示的是最佳猜测;
   - 评分侧已约定:这两类标记内的内容不会被用来新增错误条目,不会因此多扣分。
4. **OCR 忠实性红线**:识别只会“照抄笔迹”,不做任何拼写/语法修正(如学生写 ich findet 就保留 ich findet);若发现识别仍被“改好”,请在复核页改回或反馈。

### 16.12 批改意见新排版(结构化阅读)

1. **每条错误独立成区**:【原句】(错误处 ❌ 标注)→【修正】(绿底 ✅)→【错因解析/小提示】(蓝底,内部再拆为 **规则 / 推理 / 说明** 子条目),条目之间以虚线分隔;
2. **三色视觉层级**:原句=蓝边、修正=绿边、解析=蓝底,明/暗主题一致;审阅页、学生版订正单、导出报告、打印视图全部同步同构;
3. **弹层同构**:批注核对弹层中原句/修正/解析也按同样分区展示。

### 16.13 启动选择班级数据 + 演示数据清理

1. **启动选择**:每次打开系统先弹出「选择班级数据」对话框——可浏览导入本地班级数据包(class_package_*.zip)后进入,或点「直接进入系统」使用现有数据;勾选“本次会话不再提示”可跳过;
2. **为什么这样更安全**:选择前不加载任何数据,避免“打开就弹密码”;若导入时数据处于加密锁定态,解锁窗会自动弹出,解锁后重试导入即可;
3. **永久关闭**:设置中心 →「基础运行」→「启动时选择班级数据包」关掉后,启动直入工作台(与历史行为一致);
4. **清除演示数据**:同面板「清除演示数据」一键删除过去演示模式产生的样例(李明/20260123 系列)任务、错因、图片与孤儿学生,真实数据不受影响(需二次确认)。
5. **数据维护区(两个按钮,与上条并列)**:「一键清除所有数据」清空全部业务数据(批改任务与报告/错因/学生与班级/考试与报告/台账/练习卷/示范学习画像)与上传图片及全部关联文件,不可恢复;系统配置与密钥不受影响。「一键恢复所有示例数据」先清空再写入一整套可直接演示的示例数据(示例班级与花名册、学生档案、24 篇已完成批改与报告、1 个待人工复核任务、1 场考试与报告、台账记录、练习卷、风格画像与示例答卷图),写完可直接在队列/审阅/分析/台账/考试等页面演示。两者均需二次确认,结果提示右侧可一键「刷新页面」查看。

### 16.14 连续审阅与队列分组(批量场景提效)

1. **审阅页「上一份 / 下一份」**:顶部信息栏新增切换按钮与「第 i / n 份」位置显示;
   - 范围与顺序:有批次→同批次内;无批次→同班级同作业;按创建顺序(第 1、2、3 份)排列;
   - 首尾自动置灰禁用;当前任务不在范围内时(如通过筛选打开)不显示按钮;
   - **键盘快捷键**:← 上一份、→ 下一份(输入框内或对话框打开时不会误触发);
   - 切换会自动重置页签/批注筛选/选中项/编辑状态(不会串数据),浏览器返回键直回队列页。
2. **批改队列分组展示**:任务表格按「**作业名称 · 班级**」自动分组,组头显示「共 N · 已完成 M」;
   - 无作业名但同批次 → 归入「批次 xxx」组;两者均无 → 归入「其他 / 历史任务」;
   - 组头可点击折叠/展开(默认全展开);组头复选框=**组内全选/取消**(与表头全选、跨组多选共存);
   - 筛选、批量重试/删除/导出报告/导出成绩/打印、轮询与空状态提示均与以前完全一致。

### 16.15 启动解锁流程(加密场景下的正确进入顺序)

1. **新的启动顺序**:打开系统 →「选择班级数据」→(选定/直接进入后)**才检查加密状态** → 已加密则此时才弹解锁窗,输入口令后进入工作台;未加密则全程无解锁弹窗;
2. **不再“打开就弹密码”**:启动阶段不会主动查询或触发加密模块;解锁也不再是“最高优先级抢占界面”,而是启动流程中的一个步骤(解锁窗仍不可绕过:不解锁无法进入);
3. **导入时遇锁**:若在锁定态下点击「导入并进入」,会弹出解锁窗(与以往一致);解锁成功页面刷新后,重新点「导入并进入」即可;
4. **会话中仍然自动弹窗**:使用过程中如果数据再次处于锁定(如手动锁定、重启后端),任何数据操作会照旧自动弹出解锁窗,解锁后刷新加载;
5. **关闭启动选择后**(设置中心关掉「启动时选择班级数据包」):维持历史行为——直入工作台,首次数据请求遇到锁定才弹解锁窗。

### 16.16 OCR 异常自动检测与自愈(兜底)

1. **它解决什么**:识别结果"结构正常但内容异常"(如畸变词、行尾断词粘连 "Vorberei-\nten"、标签行混入正文、自评“高”但内容可疑)——系统会自动发现并尝试修复;
2. **怎么判断**:加权评分(断词粘连/标签行/畸变词/占位符密度/自评不符),得分≥阈值判为异常;可在设置中心「管线 B · 识别」组调整开关、阈值与重试次数;
3. **自动修复**:管线 B 自动重跑识别(默认 1 次,更省额度);管线 A 自动整管线重跑(默认 1 次);每次重试均写入审计日志;
4. **重试后仍异常时的降级**:
   - 自评质量自动纠偏为“低”并写入原因(转录面板与复核页会出现“识别准确率较低”警示);
   - 管线 B 自动进入人工复核(默认开启,可在设置中关闭);
   - 管线 A 在报告中追加「【识别质量提示】…建议对照原图核对或重新批改」;
5. **可追溯**:每次重试/降级均写审计日志(`ocr.anomaly_retry / degraded`、`pipeline.anomaly_rerun / degraded`);重试次数上限 3,不会无限消耗模型额度。

### 16.17 macOS 版使用(便携、离线)

1. **首次制作**(需联网一次):把整个文件夹拷到 Mac → 双击「首次制作mac运行环境.command」(自动识别 Apple 芯片/Intel,下载运行时并装依赖,成功后即可完全离线);详细说明见 `docs/macOS便携版制作与使用.md`;
2. **日常使用**:双击「一键启动.command」→ 浏览器自动打开(端口 8765→8766 自动切换);结束双击「一键停止.command」或关闭服务窗口;
3. **首次被 Gatekeeper 拦下**:右键脚本 →「打开」→ 再确认「打开」(仅一次);或用 U 盘拷贝避免隔离标记;
4. **功能与 Windows 版完全一致**(同一代码、同一界面、同一数据格式);班级数据包与数据库可 Win⇄Mac 双向互导;
5. **分发给其他 Mac**:在一台 Mac 上制作完成后打包整个文件夹(约 350~400MB),解压即用(Apple 芯片/Intel 各打包一次)。

### 16.18 “应急版”一键启动(内置云端配置)

1. **适用场景**:教师一时不清楚如何自行部署模型端点,双击「应急启动」即自带云端识别(百炼 Qwen)与评分(DeepSeek)配置;
2. **使用方法**:Windows 双击「应急启动.bat」/ Mac 双击「应急启动.command」→ 浏览器自动打开;其余操作与平常完全一样;
3. **与常规/离线版的关系**:同一套代码、同一本机数据库、同一 runtime;应急版仅在本次启动的进程内注入云端配置,不会写入 backend\.env,不影响设置中心与常规配置;两者切换=换一个启动脚本;请勿同时开两个版本(同库并发写有风险);数据加密/解锁链路不受影响;
4. **密钥位置与安全**:明文保存在根目录「应急配置.env」(已加入 .gitignore);建议 mac 执行 `chmod 600 应急配置.env`、Windows 右键属性→安全仅保留当前用户;换钥只需修改该文件;请勿随包外发或上传网盘;
5. **应急版不会污染常规配置**:若之后在设置中心保存了新的端点/密钥,仍以设置中心(backend\.env)为准。

### 16.19 标准答题卷(打印模板 + 自动找平)

1. **打印**:工作台点「打印标准答题卷」→ 新窗口的打印页;务必按 **100% 比例(不缩放)** 打印并关闭"适应页面",建议 A4 普通纸;
2. **卷面**:四个角的大黑方块(16mm,整页找平基准)、信息区下方两枚小黑方块(8mm,姓名/正文分界标记)、姓名/学号拼音填写框、班级/日期/题目、9mm 行距书写线(21 行);
3. **上传后**:系统自动检测定位块并**找平/透视校正**(歪斜、梯形畸变自动纠正,四角块完整保留);未检测到方块(普通照片)则原样处理,完全不受影响;
4. **可关闭**:设置中心「标准答题卷自动找平」开关(与存档图片压缩同组);关闭后不执行找平,但姓名小图会自动改用通用裁剪;
5. **姓名/学号对应**:找平后信息区落在固定规范坐标(姓名+学号两框 ≈ x86–1086 / y310–416;分界小方块中心 y≈514),识别规则自动适配;精度实测:端正卷误差≈0px,倾斜 8° 卷 ≤18px;
6. **云校端点视觉排查**:若识别提示「未收到任何图片」类报错,说明该端点/模型疑似不支持图片输入——命令行运行 `backend\scripts\diag_vision.py --live` 可快速判定(五种载荷对比:jpg 成功而 png 失败=端点只吃 jpg;双图失败=不支持多图);处置:改用视觉模型(如 qwen-vl-plus)或把"识别"配置指向独立视觉端点(评分仍可用校方模型);
7. **发送层已统一"直接 jpg"口径**:所有发往模型的图片一律以 JPEG 发送(非 jpg 源自动转码、透明转白底),超 3.5MB 或长边 2400px 自动缩边重编码;
8. **姓名小图展示**:识别出姓名后,审阅页原图下方会显示「姓名区大预览卡」(从原图自动截取的姓名/学号区域,点击看大图),与每页图一起展示,方便快速核对。

### 16.20 上传后姓名预识别(即刻识名)

1. **体验**:提交后无需等批改完成,系统在后台立刻识别姓名/学号,约 1~3 秒内队列页/审阅页就会亮出名字(识名过程在审阅页显示"正在预识别姓名…");
2. **裁切依据定位点**:识别用图严格按卷面上检测到的**四角定位块**裁切——标准答卷先检测定位点、找平后按几何裁出姓名区(并在画布上再做一次 4/4 锚定复检);**检测不到定位点时不做任何“猜位置”的裁剪**,直接对整图识别;每次识别都会在审计日志记录裁切依据(画布锚定 / 照片定位点 / 整图);
3. **识别引擎**:优先使用你已配置的识别端点(学校 Qwen / 百炼等);端点不可用时自动退回**本地 RapidOCR**(完全离线、CPU 可跑,无需联网与训练);全部失败则静默跳过,绝不影响批改;
4. **安全**:预识别只写姓名/学号两个字段,且仅在任务未完成时写入——批改完成后的正式结果永不被覆盖;人工复核挂起(WAITING_REVIEW)期间同样先行展示;
5. **开关**:设置中心「上传后姓名预识别」(默认开启);本地引擎组件随一键安装自动装入(体积约 +150MB;缺失时自动仅用电端点);
6. **名字不对?**:预识别结果供参考,批改完成以正式识别为准;也可随时在审阅页点铅笔修正学生信息。

### 16.21 续写纸与“自动并页”

1. **适用场景**:一篇作文写不完,需要第二张纸继续写时;
2. **打印**:工作台点「打印续写纸」→ 新窗口打印页(务必 100% 比例);续写纸与首页版式差异:无姓名区、书写线从页顶开始、**页底中部有两枚小方块**(系统据此识别“我是续写页”);
3. **上传**:与首页一起放在同一批上传(ZIP/PDF 展开同样适用);系统在落盘时自动检测续写纸并**归入上一张图片的同一任务**——无需教师指定页序,一次批改、一份完整报告(与首页多页完全一致);
4. **规则说明**:按上传顺序相邻归页(一张接一张可连续 3+ 页);遇到普通页/首页则重新起一任务;
5. **安全回退**:普通照片、旧版首页、没有标记的纸一律按现有方式处理,绝不误合并;若第一批第一张就是续写纸(没有上一页可接),会按独立任务处理并在审计中提醒;
6. **小方块请保持清晰、勿涂改/遮挡**(与四角定位块同等重要);若方块残缺,该页会按普通页处理(不并页、不报错)。

### 16.22 手写模型(花名册内采集与“训练”)

1. **入口**:工作台 →「查看/更新花名册」→ 切换到「手写模型」页签;
2. **采集四步**:①生成抄写素材(一题目句+一正文句,可「打印抄写卡」);②学生用标准卷抄写并手写班级/日期/姓名/学号/题目;③把收齐的样本图一次性上传(可多份);④等待处理完成(进度条含真实百分比与剩余时间);
3. **训练的真实含义**:系统在本地提取每份样本的姓名区图像特征签名,构建“学生手写特征库”(非神经网络训练,全程本地计算);识别出的姓名会自动与花名册对账,对不上时可在清单里手动指定学生;
4. **如何使用**:训练完成后自动生效——「上传后姓名预识别」在返回名字前会参考该班级的手写特征库;
5. **档位**(设置中心 → 上传 → 手写识别增强档位):轻量仅名单匹配 / 中等加特征近邻 / 最精确多尺度近邻+学号校验;**auto 会检测本机核心数与内存给出推荐**(设置页可见),手动选择优先;
6. **建议**:每位学生 1~3 份样本效果最好;样本可随时删除重传。

### 16.23 全局等待态状态条

1. **位置**:顶栏下方,有任务时自动出现,无任务时自动隐藏;
2. **内容**:批改队列/姓名预识别/图片压缩/手写训练的真实进度百分比与预计剩余时间,以及当前阶段;
3. **“目前状态”为真实自检**:后台会实际检查数据库、存储可写性、磁盘余量、识别引擎与前端产物——不会使用安慰式假提示;若磁盘紧张或引擎未配置,这里会如实显示。

### 16.24 界面外观档位(均衡 / 极简 / 高级)

1. **切换位置**:顶栏的滑杆图标(与明/暗主题切换并列)——点开选择三档;设置中心 →「全局开关」→「界面外观档位」亦可修改,两处互相同步;
2. **三档区别**:
   - **均衡(默认)**:既有界面表现,不改变;
   - **极简**:面向老旧/低配电脑的最简界面——整站灰阶去色(仅保留低饱和的成功/错误/警告状态色)、关闭全部过场动画与点击波纹、无阴影、纯色背景、标签(芯片)改描边样式、统计图表不做入场动画;层次靠留白、描边与字重区分,操作完全不受影响;
   - **高级**:尊享质感——页面切换内容淡入、更细腻的动效曲线与控件过渡(按钮悬停微抬、图标悬停缩放、卡片光影跃升)、更丰富的柔和光影与品牌色微光、标题下渐变细线;克制而不浮夸;
3. **生效与保存**:切换**即时生效、无需刷新**;选择会记入本机并同步保存到设置中心(下次打开仍然生效);
4. **打印不受影响**:答题卷/续写纸/抄写卡/报告打印在三个档位下完全一致。

### 16.25 纯本地 OCR 模式(识别离线化)

1. **适用场景**:希望"作文识别"完全在本机完成、不把图片上传到任何云端/远程 OCR 时(评分环节不受影响,仍按所选管线执行);
2. **开启方式**:设置中心 →「管线 B · 识别(OCR)」→「本地 OCR 模式」(默认关闭);开启后管线 B 的识别改由**本地 RapidOCR** 完成(离线 CPU;与"上传后姓名预识别"同一引擎),**不调用任何云端/远程 OCR 端点**(含 OCR 端点与 Azure);
3. **效果**:识别结果照常进入评分(DeepSeek 云端评分或本地 Qwen),报告与复核流程完全一致;姓名/学号解析与预识别同源(含"依据定位点"裁剪);
4. **依赖**:需要本地 OCR 组件(随安装包含;可在「一键自检」查看"本地 OCR(RapidOCR)"一行);组件缺失时任务会明确报错提示,不会偷偷改用云端;
5. **管线 A**:该管线的识别与评分都在本地 VLM 完成,本模式对其没有影响;
6. **提示**:本地识别为逐行 OCR,对特殊标记(如模糊字提示)的能力弱于云端视觉模型;对识别要求高时可在设置中关闭本模式恢复云端识别。

### 16.26 强制性姓名识别(选文件即识)

1. **适用场景**:想在"选择文件的瞬间"就自动看到姓名/年龄,而不必等到点【开始批改】之后;
2. **开启方式**:设置中心 →「上传与图片处理」→「强制性姓名识别」(默认关闭);
3. **效果**:开启后,在工作台选择/拖拽文件(单张、批量、ZIP、PDF 均可)的瞬间,系统即用**本地引擎**(RapidOCR,离线 CPU,不调用任何云端/远程端点)自动识别,并在文件列表对应条目的位置上直接显示识别结果(如"Li Ming · 年龄 16");开启后该位置不再显示文件名(鼠标悬停在结果上可查看文件名);
4. **状态提示**:识别过程中显示"识别中…";未能识别到个人信息显示"未识别到姓名";异常时显示"识别失败(原因)";ZIP/PDF 会逐页列出(如"第 1 页 · Wang Wei · 年龄 17");
5. **与「上传后姓名预识别」的区别**:
   - 本功能:选择文件时即识别(尚未提交、不产生任务),结果仅用于列表预览;
   - 上传后姓名预识别:点【开始批改】创建任务后,后台识别并将姓名/学号写入任务(队列/审阅页展示);
   - 两者互不冲突,可同时开启;本功能默认关闭,关闭时界面与原来完全一致;
6. **性能提示**:识别在本机 CPU 上进行(首次使用需加载引擎,约数秒);批量选择较多文件时按队列逐张识别(每张约 1~5 秒),识别中可正常继续选择或移除文件。
7. **执行顺序与裁剪依据**:每一次"选文件即识"都严格按两步自动串联执行:①**先依定位点裁剪**——检测图片中的四角定位块并按其实际位置自动裁剪出姓名区(规范卷为理论锚定 4/4 检测;照片为全象限检测+找平+锚定复检,均不使用固定坐标或与定位点无关的启发式区域);②**裁剪完成后立即识别**——识别输入恒为裁剪出的图像,不会对未裁剪原图直接识别;
8. **无定位点回退**:若图片中未检测到任何定位点,则按既定约定回退为整图识别(不中断、不报错);该情形在识别依据标记为 full-image,规范卷/照片依定位点裁剪的情形分别为 canonical / landmarks(供排查对照)。

### 16.27 二维码识别与标准答题卷绑定(「上传即识」的更快通道)

1. **打印绑定卷**:工作台 →【打印标准答题卷(绑定学生二维码)】→ 选择班级与学生 → 打开打印页即可打印;卷面姓名行右侧会打印该生的身份二维码(16mm,H 级纠错),页眉显示"本卷已绑定:姓名(学号)";
2. **上传即识**:教师上传/拖拽该卷照片(单张、批量、ZIP/PDF 均可)时,系统**优先本地解码二维码**——命中即直接得到姓名/学号(通常百毫秒内,不占用 OCR 计算),文件列表即刻显示;
3. **自动回退**:未扫到二维码(如旧版空白卷、二维码被遮挡/污损)时,自动回退既有"依定位点裁剪 + 本地识别"链——旧卷与手写姓名场景完全不受影响(行为与之前一致);
4. **兼容说明**:旧版空白卷可继续使用(不绑定);新版绑定卷与旧卷可混用;二维码位于姓名行右侧、在姓名区裁剪窗之外,不改动四角定位块/次定位块/书写线与既有找平几何;
5. **排查对照**:识别结果来源标记为 qr(二维码解码)/ canonical、landmarks(依定位点裁剪)/ full-image(整图回退)——均为完全本地运算,不触任何云端/远程端点。

---

## 附录 文件与文件夹说明

打开"批改器"文件夹,你日常只需要关心这些:

| 位置 | 说明 | 能不能动 |
|---|---|---|
| **一键启动.bat** | 每天双击启动系统 | 可以双击,不要改名/删除 |
| **一键停止.bat** | 用完双击关闭系统 | 可以双击,不要改名/删除 |
| **首次安装.bat** | 只在新电脑上运行一次 | 平时不用管 |
| **使用手册.md** | 就是本手册,随时可查 | 可以打开阅读 |
| **backend\data** | 系统的"仓库"(所有数据) | **不要删!定期备份** |
| backend\.env | 配置文件(真实模型设置在这里) | 只按第十一章修改 |
| 其他文件夹(backend、frontend 等) | 系统程序文件 | 不要改动、不要删除 |

---

*本手册对应系统 2026 年 9 月版本(含班级分析、学生错题本等第二期功能)。
如有本手册未覆盖的问题,请联系系统管理员。*
